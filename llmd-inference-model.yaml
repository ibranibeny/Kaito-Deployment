apiVersion: inference.networking.x-k8s.io/v1alpha2
kind: InferenceModel
metadata:
  name: gpt-oss-20b
  namespace: default
spec:
  modelName: "gpt-oss-20b"
  # Reference to the InferencePool
  poolRef:
    name: kaito-gpt-oss-20b-pool
  # Criticality for prioritization
  criticality: Standard
